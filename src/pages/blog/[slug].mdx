---
layout: ../../layouts/Marketing.astro
title: "Introducing AcmePay (placeholder)"
description: "A quick look at our architecture decisions."
pubDate: 2025-10-01
---

export const prerender = false;

import FeatureCard from '../../components/FeatureCard.astro';

# Building a modern payments stack

We keep the stack simple: **Astro** for the site, **islands** for interactivity, and a strict a11y policy.

```ts
// example snippet
export function add(a: number, b: number) {
  return a + b;
}
